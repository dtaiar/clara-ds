{
  "component": "Button",
  "recordStatus": "experimental — first Clara Knowledge record; shape is a hypothesis to evaluate, not a committed schema",
  "slice": "01-intent-first-discovery",
  "sourceOfTruth": {
    "implementation": "app/src/components/Button.tsx",
    "tokenDecision": "docs/foundations/token-model.md, section 'Decision — Button interaction states demonstrate the need for hover/pressed'",
    "sliceDefinition": "docs/slices/01-intent-first-discovery.md"
  },

  "purpose": {
    "status": "decision",
    "value": "A single, discrete, user-triggered primary action.",
    "note": "Derives directly from docs/slices/01-intent-first-discovery.md, which names a primary action Button as part of the Explorer entry surface — not inferred from generic Button convention."
  },

  "whenToUse": {
    "status": "decision",
    "value": [
      "Trigger an explicit, primary product action the user deliberately initiates — e.g. submitting the Explorer's intent query."
    ],
    "note": "Same source as purpose. Slice 01 only demonstrates this one use."
  },

  "whenNotToUse": {
    "status": "hypothesis",
    "value": [
      "Navigation between pages or views — not demonstrated or supported by this implementation.",
      "Selecting a pre-written example intent (a suggestion chip/pill) — the relationship between chips and Button is unresolved; do not assume chips are a Button variant.",
      "Toggling a persistent on/off state — Button's only current usage (the theme-toggle checkpoint in app/src/App.tsx) is explicitly non-product code and is not evidence that Button supports toggle semantics (no aria-pressed handling exists)."
    ],
    "note": "Reasoned exclusions, not sourced from an explicit statement in the slice docs and not tested against a real alternative UI."
  },

  "variants": {
    "status": "decision",
    "value": ["primary"],
    "note": "Descriptive, not configurable: there is no `variant` prop. Button always renders this one look. Approved scope for this implementation pass — no secondary/ghost variant exists in code."
  },

  "sizes": {
    "status": "decision",
    "value": ["default"],
    "note": "Descriptive, not configurable: there is no `size` prop. Approved scope — only one size is rendered."
  },

  "states": [
    {
      "name": "default",
      "status": "implemented"
    },
    {
      "name": "hover",
      "status": "observed",
      "note": "Consumes the newly implemented color.action.primaryHover semantic token. Verified via computed backgroundColor in both themes (Playwright, built preview)."
    },
    {
      "name": "pressed",
      "status": "observed",
      "note": "Consumes the newly implemented color.action.primaryPressed semantic token via the native :active state. Verified via computed backgroundColor in both themes."
    },
    {
      "name": "focus-visible",
      "status": "observed",
      "note": "Consumes the existing color.focus.ring token, unchanged from the ADR-002 checkpoint. Verified via keyboard Tab navigation (not programmatic .focus(), which does not trigger :focus-visible in Chromium) — 2px solid outline, theme-resolved color, in both themes."
    },
    {
      "name": "disabled",
      "status": "unresolved",
      "note": "Available only because Button forwards native <button> attributes wholesale (ComponentPropsWithoutRef<\"button\">) — excluding it from the type would have required actively stripping a native attribute, which was judged artificial. No Clara-specific disabled styling or behavior exists: the button's cursor remains 'pointer' and background remains 'action.primary' even when disabled. This is a real, currently-shipped inconsistency, not a deliberate design."
    }
  ],

  "props": {
    "status": "implemented",
    "value": [
      { "name": "children", "type": "ReactNode", "required": true, "description": "Visible text label. Also the accessible name — there is no separate label prop." },
      { "name": "onClick", "type": "(event: MouseEvent) => void", "required": false },
      { "name": "type", "type": "\"button\" | \"submit\" | \"reset\"", "required": false, "default": "button" },
      { "name": "disabled", "type": "boolean", "required": false, "description": "Forwarded natively; see states[].disabled for what this does and does not do." },
      { "name": "...rest", "type": "native <button> element attributes", "required": false, "description": "All other standard <button> attributes (aria-*, id, data-*, etc.) pass through unchanged." }
    ],
    "note": "Deliberately no variant, size, icon, loading, or destructive props — none are implemented. Types here are a simplified paraphrase for readability; app/src/components/Button.tsx (ButtonProps = ComponentPropsWithoutRef<\"button\">) is the exact and complete source of truth, including native attributes not itemized above."
  },

  "accessibility": [
    { "claim": "Renders a native <button> element.", "status": "decision" },
    { "claim": "Accessible name equals the visible text label (children).", "status": "decision", "note": "No icon-only usage is in scope, so no aria-label prop was added." },
    { "claim": "Visible focus-visible outline using color.focus.ring, 2px solid, theme-resolved.", "status": "observed", "note": "Verified via keyboard-driven computed style check in both themes." },
    { "claim": "Keyboard activation (Enter/Space) and screen-reader announcement behavior.", "status": "unresolved", "note": "Relies entirely on native <button> semantics; not independently tested with a screen reader or assistive technology by this implementation pass. Do not read the other accessibility claims here as a compliance statement." }
  ],

  "semanticTokens": {
    "status": "observed",
    "value": [
      "color.action.primary",
      "color.action.primaryHover",
      "color.action.primaryPressed",
      "color.action.onPrimary",
      "color.focus.ring",
      "space.4",
      "space.2",
      "radii.md",
      "textStyle: label"
    ],
    "note": "Confirmed present in the generated token vocabulary (styled-system/tokens/tokens.d.ts) after panda codegen, and confirmed resolving to theme-specific values in the generated CSS ([data-panda-theme=...] blocks) rather than leaking into Button's own component-level CSS classes."
  },

  "typography": {
    "status": "decision",
    "value": "textStyle: label",
    "note": "Reused Clara's existing 'label' text style (sm size / medium weight) rather than introducing a new role. No semantic distinction was found that would justify a dedicated Button typography role."
  },

  "knownLimitations": {
    "status": "decision",
    "value": [
      "One visual variant, one size — no secondary/ghost variant, no size scale.",
      "No icon, loading, or destructive support.",
      "No dedicated disabled visual treatment — disabled is only available via native prop forwarding; cursor and background do not change when disabled.",
      "No ButtonGroup or other compound composition.",
      "Relationship to suggestion chips/pills is undecided — do not assume equivalence or reuse without a separate decision.",
      "Hover/pressed color values are an unvalidated placeholder methodology (uniform RGB darkening of the theme accent), not a contrast-checked pair — consistent with token-model.md's existing position that exact color values are not yet approved.",
      "No automated or manual accessibility testing beyond the two specific claims marked 'observed' above."
    ]
  },

  "unresolved": [
    "Whether disabled should get dedicated Clara styling/behavior, or stay purely native.",
    "Whether suggestion chips are a Button variant, a separate component, or unrelated.",
    "Whether hover/pressed values meet any contrast requirement — not evaluated.",
    "antiPatterns, relatedComponents, examples, and compositionRules are not included in this record: Button has not yet been used in enough real contexts (no eval loop, no second component, no built Explorer surface) to observe any of them honestly. Adding them now would be invention, not evidence."
  ]
}
